### Padrão de Observador (Observer Pattern):

[pt-br] O padrão de observador permite que um objeto (sujeito) mantenha uma lista de objetos dependentes (observadores) que são notificados sobre as mudanças de estado.

[en] The observer pattern allows an object (subject) to maintain a list of dependent objects (observers) that are notified about state changes.
